# @vessel-dsp/cabinet

Generated speaker cabinet preview helpers for VesselDSP profile data.

The package accepts a small cabinet profile with brand, model, enclosure color,
dimensions, and speaker count, then creates deterministic layout data,
a Three.js object graph, and a GLB preview payload.

Shared preview effects are delegated to `@vessel-dsp/visual-effects`.
